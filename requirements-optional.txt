# Optional Jarvis dependencies — pip install -r requirements-optional.txt
# Note: torch (~2GB+) is pulled in by whisper/audiocraft.
# NVIDIA: default pip torch. AMD RX: run ./scripts/install-rocm-pytorch.sh then JARVIS_TORCH_DEVICE=cuda
openai-whisper>=20231117
faster-whisper>=1.0.0
librosa>=0.10.0
matplotlib>=3.8.0
weasyprint>=62.0
pandas>=2.1.0
openpyxl>=3.1.0
httpx>=0.27.0
httpx2>=0.1.0  # Starlette TestClient (pytest) — silences httpx deprecation warning
ddgs>=9.0.0
duckduckgo-search>=6.0.0
pymupdf>=1.24.0
python-docx>=1.1.0
pytest>=8.0.0
ruff>=0.8.0
python-lsp-server>=1.11.0
mypy>=1.13.0
mcp>=1.0
# MusicGen (optional, no audiocraft/spacy): pip install transformers scipy accelerate
transformers>=4.40.0
scipy>=1.11.0
accelerate>=0.30.0
openwakeword>=0.4.0
# AI vocals + diarization (optional; HF_TOKEN needed for pyannote models):
# pip install git+https://github.com/suno-ai/bark.git
# pip install coqui-tts
# pip install pyannote.audio
# Legacy MusicGen backend (often fails on Py3.12 — pulls spacy/thinc, pins torch 2.1):
# audiocraft>=1.3.0
